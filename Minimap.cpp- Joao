int startX - this->charactcr->getX() - 2; 
if (startX < 0)
startX - 0;
int startY = this->character->getY() - 2; 
if (startY < 0)
startY = 0;
int endx = this-character->getx() + 2; 
int endY = this->character->getY() + 2;
stringstream« ss;
for (size_t y - startY; y <- endY; y++)
{
for (sizc_t x - startY; x <- endY; x++)
{
srand(x + y);
int location - rand() % 3;
if (x == = this->character->getX() Jtt y = = this->character->getY())
ss « "p";
else
{
switch (location)
{
case EMPTY
ss « "E"; 
break;
case ATLANTIS
ss « "A"; 
break;
case ISLAND Of FLAME 
ss « "F "; 
break;
case PHOCNIX ISLE
ss « "P '; 
break;

